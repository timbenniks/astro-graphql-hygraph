---
interface Props {
  description: string;
  image: string;
  title: string;
  url: string;
}

const { description, image, title, url } = Astro.props;
---

<li>
  <a href={url} target="_blank">
    <h3 class="line-clamp-1 font-bold text-2xl">{title}</h3>
    <p class="line-clamp-1">{description}</p>
    <img
      src={image}
      loading="lazy"
      alt={title}
      class="aspect-video object-cover"
    />
  </a>
</li>
